<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.bonitasoft.engine.data.model.impl.SDataSourceParameterImpl">
    <cache/>
	<insert id="insertSDataSourceParameterImpl" parameterType="dataSourceParameter">
		INSERT INTO datasourceparameter (tenantid, id, dataSourceId, name, value_)
		VALUES (#{tenantid}, #{entity.id}, #{entity.dataSourceId}, #{entity.name}, #{entity.value_})
	</insert>

	<delete id="deleteSDataSourceParameterImpl" parameterType="dataSourceParameter">
		DELETE FROM datasourceparameter
		WHERE tenantid = #{tenantid}
		AND id = #{id}
	</delete>

	<resultMap id="dataSourceParameterResultMap" type="dataSourceParameter">
		<id property="id" column="id" />
		<result property="dataSourceId" column="dataSourceId" />
		<result property="name" column="name" />
		<result property="value_" column="value_" />
	</resultMap>

	<select id="getDataSourceParameterByNameAndDataSourceId" parameterType="long" resultMap="dataSourceParameterResultMap">
		SELECT *
		FROM datasourceparameter datasourceparameter
		WHERE datasourceparameter.tenantid = #{tenantid}
		AND datasourceparameter.name = #{name}
		AND datasourceparameter.dataSourceId = #{dataSourceId}
		<if test="orderBy != null">
			ORDER BY ${orderBy}
		</if>
	</select>

	<select id="getDataSourceParameterById" parameterType="long" resultMap="dataSourceParameterResultMap">
		SELECT *
		FROM datasourceparameter
		WHERE tenantid = #{tenantid}
		AND id = #{id}
	</select>

	<select id="getDataSourceParametersByDataSourceId" parameterType="long" resultMap="dataSourceParameterResultMap">
		SELECT *
		FROM datasourceparameter datasourceparameter
		WHERE datasourceparameter.tenantid = #{tenantid}
		AND datasourceparameter.dataSourceId = #{dataSourceId}
		<if test="orderBy != null">
			ORDER BY ${orderBy}
		</if>
	</select>

</mapper>
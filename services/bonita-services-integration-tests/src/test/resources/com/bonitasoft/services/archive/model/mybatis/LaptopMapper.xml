<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.bonitasoft.engine.archive.model.Laptop">

	<select id="getLaptopById" parameterType="long" resultMap="laptopResultMap">
   		SELECT laptop.*
		FROM laptop
		WHERE id = #{id}
		AND tenantid = #{tenantid}
	</select>

	<insert id="insertLaptop" parameterType="laptop">
		INSERT INTO laptop (TENANTID, ID, brand, model,archivedate)
		VALUES (#{tenantid}, #{entity.id}, #{entity.brand}, #{entity.model},#{entity.archiveDate})
	</insert>

	<delete id="deleteLaptop" parameterType="laptop">
		DELETE FROM laptop
		WHERE tenantid = #{tenantid}
      AND id = #{id} 
	</delete> 
	
	<resultMap id="laptopResultMap" type="laptop">
		<id property="id" column="ID" />
		<result property="brand" column="BRAND" />
		<result property="model" column="MODEL" />
		<result property="archiveDate" column="ARCHIVEDATE" />
	</resultMap>

</mapper>
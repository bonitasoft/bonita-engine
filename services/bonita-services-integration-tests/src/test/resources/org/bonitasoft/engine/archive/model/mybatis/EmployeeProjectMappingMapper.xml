<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.bonitasoft.engine.archive.model.EmployeeProjectMapping">

	<select id="getEmployeeProjectMappingById" parameterType="long" resultMap="employeeProjectMappingResultMap">
		SELECT *
		FROM employeeprojectmapping
		WHERE id = #{id}
		AND tenantid = #{tenantid}
	</select>

	<insert id="insertEmployeeProjectMapping" parameterType="employeeProjectMapping">
		INSERT INTO employeeprojectmapping (tenantid, id, employeeId, projectId, archivedate)
		VALUES (#{tenantid}, #{entity.id}, #{entity.employeeId}, #{entity.projectId},#{entity.archiveDate})
	</insert>

	<delete id="deleteEmployeeProjectMapping" parameterType="employeeProjectMapping">
		DELETE FROM employeeprojectmapping
		WHERE tenantid = #{tenantid}
		AND id = #{id}
	</delete>

	<resultMap id="employeeProjectMappingResultMap" type="employeeProjectMapping">
		<id property="id" column="id" />
		<result property="employeeId" column="employeeid" />
		<result property="projectId" column="projectid" />
		<result property="archiveDate" column="archivedate" />
	</resultMap>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.bonitasoft.engine.archive.model.Address">

	<select id="getAddressById" parameterType="long" resultMap="addressResultMap">		
		SELECT address.*
		FROM address
		WHERE id = #{id}
	</select>

	<insert id="insertAddress" parameterType="address">
		INSERT INTO address (TENANTID, ID, address, employeeId, archivedate)
		VALUES (#{tenantid}, #{entity.id}, #{entity.address}, #{entity.employeeId}, #{entity.archiveDate})
	</insert>

	<delete id="deleteAddress" parameterType="address">
		DELETE FROM address
		WHERE tenantid = #{tenantid}
      AND id = #{id} 
	</delete>
	
	<resultMap id="addressResultMap" type="address">
		<id property="id" column="ID" />
		<result property="address" column="ADDRESS" />
		<result property="employeeId" column="EMPLOYEEID" />
		<result property="archiveDate" column="ARCHIVEDATE" />
	</resultMap>

</mapper>
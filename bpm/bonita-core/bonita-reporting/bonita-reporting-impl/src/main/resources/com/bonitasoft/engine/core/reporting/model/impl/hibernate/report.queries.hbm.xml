<?xml version="1.0"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">
<hibernate-mapping auto-import="false">

   <query name="getReportById">
		SELECT report
		FROM com.bonitasoft.engine.core.reporting.SReportImpl AS report
		WHERE report.id = :id
	</query>

   <query name="getReportByName">
		SELECT report
		FROM com.bonitasoft.engine.core.reporting.SReportImpl AS report
		WHERE report.name = :reportName
	</query>

	<query name="getNumberOfSReport">
		SELECT COUNT(report)
		FROM com.bonitasoft.engine.core.reporting.SReportImpl AS report
	</query>

	<query name="searchSReport">
		SELECT report
		FROM com.bonitasoft.engine.core.reporting.SReportImpl AS report
	</query>

	<query name="getReportContent">
		SELECT reportcontent
		FROM com.bonitasoft.engine.core.reporting.SReportContentImpl AS reportcontent
		WHERE reportcontent.id = :id
	</query>

</hibernate-mapping>

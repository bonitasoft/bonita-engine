<project name="bonita-integration-tests-services-bpm-sp" default="cleantarget">

	<property name="target.dir" value="${basedir}/target"/>
	
	<target name="cleantarget">
		<delete dir="${target.dir}" includes="*.db" />
		<delete dir="${target.dir}/home" />
		
		<copy todir="${target.dir}/home">
			<fileset dir="${basedir}/home" />
		</copy>

		<copy todir="${target.dir}/home/server/licenses" overwrite="true">
			<fileset dir="${user.home}/lic" includes="*.lic" />
		</copy>

		<delete dir="${target.dir}/test-classes/conf" />
		<copy todir="${target.dir}/test-classes/conf">
			<fileset dir="${basedir}/../../../bonita-engine/bonita-integration-tests/bonita-test-utils/bonita-server-test-utils/src/test/resources/conf/service" />
		</copy>
		<!-- Need this order to overwrite existing files -->
		<copy todir="${target.dir}/test-classes/conf" overwrite="true">
			<fileset dir="${basedir}/../../../bonita-engine/bonita-integration-tests/bonita-test-utils/bonita-server-test-utils/src/test/resources/conf/bpm" />
		</copy>
		<copy todir="${target.dir}/test-classes/conf" overwrite="true">
			<fileset dir="${basedir}/src/test/resources/conf" />
		</copy>

		<!-- sql files -->
		<delete dir="${target.dir}/server" />
    	<delete dir="${target.dir}/server/sql" />
    	<copy todir="${target.dir}/server/sql">
    		<fileset dir="${basedir}/../../../bonita-engine/bpm/bonita-sql/sql" />
    	</copy>

		<loadfile srcFile="${basedir}/pom.xml" property="project.version">
			<filterchain>
				<linecontains>
					<contains value="version>"/>
				</linecontains>
				<headfilter lines="1"/>
				<replaceregex pattern=".*&lt;version&gt;(.*)&lt;/version&gt;.*" replace="\1" />
			</filterchain>
		</loadfile>
		<echo file="${target.dir}/home/server/platform/conf/VERSION" append="false">${project.version}</echo>
	</target>

</project>